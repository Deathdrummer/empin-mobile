# Task 4: Интеграция средних кнопок для раздела "План-график работ"

## Цель
Добавить средние кнопки "Календарь" и "Фильтры" для раздела "План-график работ", сохранив существующую функциональность.

## Контекст
Средние кнопки (позиции 1-2) должны отображаться только когда активен раздел Timesheet.
Нужно сохранить работу календаря, фильтров, индикатора активных фильтров.

## Файлы
- `src/components/BottomMenu.js`
- `src/screens/Timesheet/index.js` (интеграция)

## План
1. [x] В renderMiddleButtons() добавить case для раздела Timesheet
2. [x] Позиция 1: кнопка "Календарь" (Ionicons `calendar-outline`)
3. [x] Позиция 2: кнопка "Фильтры" (Ionicons `funnel-outline`) с индикатором активных фильтров
4. [x] Подключить пропсы: `onCalendarPress`, `onFilterPress`, `hasActiveFilters`, `onClearFilters`
5. [x] Обновить Timesheet/index.js для передачи всех необходимых пропсов
6. [x] Старые пропсы `showCalendar`, `showFilter` не использовались (проверено)

## Прогресс
- Реализован case для `section === 'timesheet'` в renderMiddleButtons() (BottomMenu.js:88-111)
- Средние кнопки корректно отображают иконки "Календарь" и "Фильтры"
- Индикатор активных фильтров с кнопкой очистки работает
- Все пропсы переданы из Timesheet/index.js (строки 491-500)

## Результат
- **Изменено:** Нет изменений (функционал был реализован ранее)
- **Ключевые изменения:**
  - BottomMenu.js: case для timesheet с кнопками "Календарь" и "Фильтры"
  - Timesheet/index.js: все необходимые пропсы переданы в BottomMenu

## Критерии готовности
- [x] В разделе Timesheet средние кнопки "Календарь" и "Фильтры" отображаются
- [x] Кнопка "Календарь" открывает календарь
- [x] Кнопка "Фильтры" открывает фильтры
- [x] Индикатор активных фильтров работает
- [x] Тап на индикатор очищает фильтры
