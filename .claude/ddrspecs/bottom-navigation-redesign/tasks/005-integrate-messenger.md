# Task 5: Интеграция средних кнопок для раздела "Мессенджер"

## Цель
Добавить средние кнопки "Чаты" и "История звонков" для раздела "Мессенджер", сохранив существующую функциональность переключения подразделов.

## Контекст
Средние кнопки (позиции 1-2) должны отображаться только когда активен раздел Messenger.
Нужно сохранить работу переключения между "Чаты" и "История звонков", подсветку активной кнопки.

## Файлы
- `src/components/BottomMenu.js`
- `src/screens/MessengerScreen.js` (интеграция)

## План
1. [x] В renderMiddleButtons() добавить case для раздела Messenger
2. [x] Позиция 1: кнопка "Чаты" (Ionicons `chatbubble-outline`) с подсветкой если активна
3. [x] Позиция 2: кнопка "История звонков" (Ionicons `call-outline`) с подсветкой если активна
4. [x] Подключить пропсы: `onNavigateToChats`, `onNavigateToCallHistory`, `currentScreen`
5. [x] Обновить MessengerScreen.js для передачи всех необходимых пропсов
6. [x] Старые пропсы не использовались (проверено)

## Прогресс
- Реализован case для `section === 'messenger'` в renderMiddleButtons() (BottomMenu.js:112-142)
- Средние кнопки корректно отображают иконки "Чаты" и "История звонков"
- Активная кнопка подсвечивается через `currentScreen` ('Chats' или 'CallHistory')
- Стили активной кнопки: background (#e8e8e8) + shadow + borderRadius (строки 194-206)
- Все пропсы переданы из MessengerScreen.js (строки 71-78)

## Результат
- **Изменено:** Нет изменений (функционал был реализован ранее)
- **Ключевые изменения:**
  - BottomMenu.js: case для messenger с кнопками "Чаты" и "История звонков"
  - MessengerScreen.js: все необходимые пропсы переданы в BottomMenu
  - Подсветка активной кнопки работает корректно

## Критерии готовности
- [x] В разделе Messenger средние кнопки "Чаты" и "История звонков" отображаются
- [x] Кнопка "Чаты" переключает на подраздел "Чаты"
- [x] Кнопка "История звонков" переключает на подраздел "История звонков"
- [x] Активная кнопка подсвечивается (background + тень)
- [x] Переключение работает корректно
